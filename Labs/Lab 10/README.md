What is the fundamental difference between supervised and unsupervised learning?

The fundamental difference is the presence of labels. Supervised learning uses a labeled dataset (features $X$ and a target $y$) to learn a mapping. Unsupervised learning works with unlabeled data (features $X$ only) to discover hidden structures, such as groups or patterns, without being told what the "correct" answer is.In the K-Means elbow plot, why don't we just choose the largest possible k to get the lowest inertia?If we set $k$ equal to the number of data points, the inertia would be 0 because every point would be its own cluster center. However, this is overfitting; the model would fail to find meaningful groups and lose its ability to generalize. We look for the "elbow" to find the point where adding more clusters no longer significantly improves the fit.In the PCA results, the first two principal components explained over 95% of the variance. What does this tell you about the original 4-dimensional Iris dataset?This tells us that the original 4 dimensions (features) are highly correlated. Since nearly all the information (variance) is captured in just two components, it means the dataset is essentially two-dimensional in nature, and the other two dimensions are largely redundant.